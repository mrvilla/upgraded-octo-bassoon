import { Meta } from "@storybook/addon-docs/blocks";
import { PrimitiveCatalog } from "../components/PrimitiveCatalog.tsx";

<Meta title="Foundations/Colors/Primitives" />

# Primitive colors

Primitive palettes come from `tokens/primitive/color.json`. Swatches use the resolved hex value, not a semantic CSS variable, so they stay the same in Light and Dark.

Each swatch lists the DTCG path, CSS custom property, and hex value.

<PrimitiveCatalog />
